import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    mainTextStyle: {
        fontSize: 20,
        textAlign: 'center',
        backgroundColor: '#f2f'
    }
})

export default styles;